---
dependency:
  name: galaxy
  enabled: true
  options:
    ignore-certs: false
    ignore-errors: false
    role-file: requirements.yml
    requirements-file: requirements.yml
driver:
  name: docker
platforms:
  - name: debian_role_bitwarden_desktop_default
    image: debian
    hostname: debian_role_bitwarden_desktop_defaults
    dockerfile: Dockerfile.j2
    pre_build_image: false
provisioner:
  name: ansible
  playbooks:
    converge: converge.yml
scenario:
  name: role.bitwarden_desktop.default
  create_sequence:
    - create
  converge_sequence:
    - converge
  test_sequence:
    - lint
    - syntax
    - create
    - converge
    - idempotence
    - destroy
verifier:
  name: ansible
lint: |
  set -e
  yamllint roles/bitwarden_desktop
  ansible-lint roles/bitwarden_desktop
  flake8 roles/bitwarden_desktop
