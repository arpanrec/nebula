---
- name: Scripts | Molecule | Converge
  hosts: all
  gather_facts: false
  tasks:
    - name: Scripts | Molecule | Converge | Include util_scripts
      ansible.builtin.import_role:
        name: arpanrec.utilities.util_scripts
